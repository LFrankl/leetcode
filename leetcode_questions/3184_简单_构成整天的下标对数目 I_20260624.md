# 3184. 构成整天的下标对数目 I

**难度**: Easy

**标签**: 数组, 哈希表, 计数

**链接**: https://leetcode.cn/problems/count-pairs-that-form-a-complete-day-i/

---

## 题目描述

<p>给你一个整数数组 <code>hours</code>，表示以 <strong>小时 </strong>为单位的时间，返回一个整数，表示满足 <code>i &lt; j</code> 且 <code>hours[i] + hours[j]</code> 构成 <strong>整天 </strong>的下标对&nbsp;<code>i</code>, <code>j</code> 的数目。</p>

<p><strong>整天 </strong>定义为时间持续时间是 24 小时的 <strong>整数倍 </strong>。</p>

<p>例如，1 天是 24 小时，2 天是 48 小时，3 天是 72 小时，以此类推。</p>

<p>&nbsp;</p>

<p><strong class="example">示例 1：</strong></p>

<div class="example-block">
<p><strong>输入：</strong> <span class="example-io">hours = [12,12,30,24,24]</span></p>

<p><strong>输出：</strong> <span class="example-io">2</span></p>

<p><strong>解释：</strong></p>

<p>构成整天的下标对分别是 <code>(0, 1)</code> 和 <code>(3, 4)</code>。</p>
</div>

<p><strong class="example">示例 2：</strong></p>

<div class="example-block">
<p><strong>输入：</strong> <span class="example-io">hours = [72,48,24,3]</span></p>

<p><strong>输出：</strong> <span class="example-io">3</span></p>

<p><strong>解释：</strong></p>

<p>构成整天的下标对分别是 <code>(0, 1)</code>、<code>(0, 2)</code> 和 <code>(1, 2)</code>。</p>
</div>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li><code>1 &lt;= hours.length &lt;= 100</code></li>
	<li><code>1 &lt;= hours[i] &lt;= 10<sup>9</sup></code></li>
</ul>


---

## 代码模板


### C++

```cpp
class Solution {
public:
    int countCompleteDayPairs(vector<int>& hours) {
        
    }
};
```

### Java

```java
class Solution {
    public int countCompleteDayPairs(int[] hours) {
        
    }
}
```

### Python3

```python3
class Solution:
    def countCompleteDayPairs(self, hours: List[int]) -> int:
        
```

### JavaScript

```javascript
/**
 * @param {number[]} hours
 * @return {number}
 */
var countCompleteDayPairs = function(hours) {
    
};
```

### Go

```golang
func countCompleteDayPairs(hours []int) int {
    
}
```

---

## 提示

1. Brute force all pairs <code>(i, j)</code> and check if they form a valid complete day. It is considered a complete day if <code>(hours[i] + hours[j]) % 24 == 0</code>.

---

## 示例测试用例

```
[12,12,30,24,24]
```

---

## AI 解答 (DeepSeek)

### 解法一：暴力枚举

思路：枚举所有满足 i < j 的下标对，判断 `hours[i] + hours[j]` 是否为 24 的整数倍，即 `(hours[i] + hours[j]) % 24 == 0`。数据规模 n ≤ 100，暴力完全可行。

```go
func countCompleteDayPairs(hours []int) int {
    count := 0
    for i := 0; i < len(hours); i++ {
        for j := i + 1; j < len(hours); j++ {
            if (hours[i] + hours[j]) % 24 == 0 {
                count++
            }
        }
    }
    return count
}
```

复杂度：时间 O(n²)，空间 O(1)

---

### 解法二：余数计数（最优）

思路：`(hours[i] + hours[j]) % 24 == 0` 等价于 `hours[i] % 24 + hours[j] % 24 == 24`（或两者余数均为 0）。先将每个元素对 24 取余，用数组计数余数出现次数，然后对余数 r，找配对余数 `(24 - r) % 24` 的数量即可。注意余数 0 和 12 的自配对需要特殊处理（组合数 C(cnt, 2)）。

```go
func countCompleteDayPairs(hours []int) int {
    cnt := [24]int{}
    for _, h := range hours {
        cnt[h % 24]++
    }
    // 余数 0 的自配对：C(cnt[0], 2)
    // 余数 12 的自配对：C(cnt[12], 2)
    // 余数 r 与余数 24-r 配对（1<=r<=11）
    res := cnt[0] * (cnt[0] - 1) / 2
    res += cnt[12] * (cnt[12] - 1) / 2
    for r := 1; r <= 11; r++ {
        res += cnt[r] * cnt[24 - r]
    }
    return res
}
```

复杂度：时间 O(n)，空间 O(1)（余数数组大小固定为 24）

---

*获取时间: 2026-06-24 10:00:21*
