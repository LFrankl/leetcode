# 1141. 查询近30天活跃用户数

**难度**: Easy

**标签**: 数据库

**链接**: https://leetcode.cn/problems/user-activity-for-the-past-30-days-i/

---

## 题目描述

<p>表：<code>Activity</code></p>

<pre>
+---------------+---------+
| Column Name   | Type    |
+---------------+---------+
| user_id       | int     |
| session_id    | int     |
| activity_date | date    |
| activity_type | enum    |
+---------------+---------+
该表可能包含重复数据。
activity_type 列是 ENUM(category) 类型， 从 ('open_session'， 'end_session'， 'scroll_down'， 'send_message') 取值。
该表记录社交媒体网站的用户活动。
注意，每个会话只属于一个用户。
</pre>

<p>&nbsp;</p>

<p>编写解决方案，统计截至&nbsp;<code>2019-07-27</code>（包含2019-07-27），近<strong>&nbsp;</strong><code>30</code> 天的每日活跃用户数（当天只要有一条活动记录，即为活跃用户）。</p>

<p>以 <strong>任意顺序</strong> 返回结果表。</p>

<p>结果示例如下。</p>

<p><strong>注意</strong>：（<code>'open_session'</code>，<code>'end_session'</code>，<code>'scroll_down'</code>，<code>'send_message'</code>）中的任何活动将被视为用户在某一天活跃的有效活动。</p>

<p>&nbsp;</p>

<p><strong>示例 1:</strong></p>

<pre>
<strong>输入：</strong>
Activity table:
+---------+------------+---------------+---------------+
| user_id | session_id | activity_date | activity_type |
+---------+------------+---------------+---------------+
| 1       | 1          | 2019-07-20    | open_session  |
| 1       | 1          | 2019-07-20    | scroll_down   |
| 1       | 1          | 2019-07-20    | end_session   |
| 2       | 4          | 2019-07-20    | open_session  |
| 2       | 4          | 2019-07-21    | send_message  |
| 2       | 4          | 2019-07-21    | end_session   |
| 3       | 2          | 2019-07-21    | open_session  |
| 3       | 2          | 2019-07-21    | send_message  |
| 3       | 2          | 2019-07-21    | end_session   |
| 4       | 3          | 2019-06-25    | open_session  |
| 4       | 3          | 2019-06-25    | end_session   |
+---------+------------+---------------+---------------+
<strong>输出：</strong>
+------------+--------------+ 
| day        | active_users |
+------------+--------------+ 
| 2019-07-20 | 2            |
| 2019-07-21 | 2            |
+------------+--------------+ <strong>
解释：</strong>注意非活跃用户的记录不需要展示。</pre>


---

## 代码模板


---

## 示例测试用例

```
{"headers":{"Activity":["user_id","session_id","activity_date","activity_type"]},"rows":{"Activity":[[1,1,"2019-07-20","open_session"],[1,1,"2019-07-20","scroll_down"],[1,1,"2019-07-20","end_session"],[2,4,"2019-07-20","open_session"],[2,4,"2019-07-21","send_message"],[2,4,"2019-07-21","end_session"],[3,2,"2019-07-21","open_session"],[3,2,"2019-07-21","send_message"],[3,2,"2019-07-21","end_session"],[4,3,"2019-06-25","open_session"],[4,3,"2019-06-25","end_session"]]}}
```

---

## AI 解答 (DeepSeek)

## 1️⃣ 题意理解

**核心要求**：
统计截至 2019-07-27（包含当天）的近 30 天内，每天的活跃用户数。只要用户在某天有任意活动记录，就视为该天的活跃用户。

**关键约束**：
1. 时间范围：`activity_date` 必须在 `2019-07-27` 之前的 30 天内（包含 2019-07-27）
2. 活跃定义：当天有任意 `activity_type` 记录即算活跃
3. 去重：同一用户在同一天可能有多个活动，只计一次
4. 输出：只输出有活跃用户的日期，按任意顺序

**边界情况**：
- 日期范围可能为空（无符合条件的数据）
- 同一天同一用户有多个活动需要去重
- 日期可能不连续，只输出有活跃用户的日期

**输入输出**：
- 输入：Activity 表，包含 user_id, session_id, activity_date, activity_type
- 输出：包含两列的表：day（日期）, active_users（活跃用户数）

## 2️⃣ 解题思路

### 解法一：基础筛选 + 分组统计
**核心思想**：
1. 先筛选出符合日期范围的数据
2. 按日期和用户去重
3. 按日期分组统计用户数

**步骤**：
1. WHERE 子句筛选日期范围：`activity_date BETWEEN '2019-06-28' AND '2019-07-27'`
2. 使用 DISTINCT 或 GROUP BY 对 (activity_date, user_id) 去重
3. 按 activity_date 分组，COUNT(DISTINCT user_id) 或 COUNT(*) 统计
4. 重命名列并输出

**注意事项**：
- 日期范围计算：2019-07-27 往前推 29 天是 2019-06-28（共30天）
- 使用 DATE_SUB 或 DATE_ADD 函数确保日期计算准确

### 解法二：子查询去重 + 分组
**核心思想**：
先通过子查询获取去重后的每日活跃用户列表，再分组统计

**步骤**：
1. 子查询中筛选日期并去重
2. 外层查询按日期分组统计
3. 更清晰但可能效率略低

## 3️⃣ 代码实现

### 解法一：直接筛选分组
```go
// SQL 实现（题目要求 SQL 查询）
SELECT 
    activity_date AS day,
    COUNT(DISTINCT user_id) AS active_users
FROM Activity
WHERE activity_date BETWEEN DATE_SUB('2019-07-27', INTERVAL 29 DAY) AND '2019-07-27'
GROUP BY activity_date
ORDER BY activity_date;
```

### 解法二：子查询方式
```go
// SQL 实现
SELECT 
    day,
    COUNT(*) AS active_users
FROM (
    SELECT DISTINCT
        activity_date AS day,
        user_id
    FROM Activity
    WHERE activity_date BETWEEN DATE_SUB('2019-07-27', INTERVAL 29 DAY) AND '2019-07-27'
) AS distinct_users
GROUP BY day
ORDER BY day;
```

## 4️⃣ 复杂度分析

### 解法一：
- ⏱️ **时间复杂度**：O(n)，其中 n 是表中记录数。需要扫描一次表进行筛选和分组。
- 💾 **空间复杂度**：O(m)，其中 m 是不同 (date, user_id) 组合数。分组操作需要临时存储。

### 解法二：
- ⏱️ **时间复杂度**：O(n)，同样需要扫描表，子查询可能增加额外开销。
- 💾 **空间复杂度**：O(m)，子查询需要存储去重结果。

## 5️⃣ 解法对比

| 解法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 解法一 | 简洁高效，单次扫描完成 | 逻辑稍紧凑 | 大多数情况，推荐使用 |
| 解法二 | 逻辑清晰，易于理解 | 可能有额外子查询开销 | 需要更易读的代码时 |

**推荐**：解法一。更简洁高效，符合 SQL 最佳实践。

## 6️⃣ 测试用例

### 用例1：常规用例
**输入**：
```
Activity 表：
user_id | session_id | activity_date | activity_type
--------|------------|---------------|--------------
1       | 1          | 2019-07-20    | open_session
1       | 1          | 2019-07-20    | scroll_down
2       | 4          | 2019-07-20    | open_session
2       | 4          | 2019-07-21    | send_message
3       | 2          | 2019-07-21    | open_session
4       | 3          | 2019-06-25    | open_session  # 超出30天范围
```

**输出**：
```
day         | active_users
------------|-------------
2019-07-20  | 2
2019-07-21  | 2
```

**解释**：2019-06-25 超出范围，不统计。2019-07-20 有用户1和2，2019-07-21 有用户2和3。

### 用例2：边界情况 - 无符合条件数据
**输入**：
```
Activity 表：
user_id | session_id | activity_date | activity_type
--------|------------|---------------|--------------
1       | 1          | 2019-06-01    | open_session  # 超出范围
2       | 2          | 2019-06-15    | send_message  # 超出范围
```

**输出**：
```
day | active_users
----|-------------
(空结果集)
```

**解释**：所有记录都超出30天范围，输出空表。

### 用例3：同用户同天多次活动
**输入**：
```
Activity 表：
user_id | session_id | activity_date | activity_type
--------|------------|---------------|--------------
1       | 1          | 2019-07-27    | open_session
1       | 1          | 2019-07-27    | scroll_down
1       | 1          | 2019-07-27    | send_message
1       | 1          | 2019-07-27    | end_session
```

**输出**：
```
day         | active_users
------------|-------------
2019-07-27  | 1
```

**解释**：同一用户同一天多个活动，只计为1个活跃用户。

## 7️⃣ 相关题目
1. **LeetCode 1141. 查询近30天活跃用户数**（本题）
2. **LeetCode 1142. 过去30天的用户活动 II**（类似，但计算平均会话数）
3. **LeetCode 550. 游戏玩法分析 IV**（用户留存分析）
4. **LeetCode 1097. 游戏玩法分析 V**（用户首次登录后的活动）

---

*获取时间: 2026-03-10 10:31:13*
