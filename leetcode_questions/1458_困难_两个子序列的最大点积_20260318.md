# 1458. 两个子序列的最大点积

**难度**: Hard

**标签**: 数组, 动态规划

**链接**: https://leetcode.cn/problems/max-dot-product-of-two-subsequences/

---

## 题目描述

<p>给你两个数组&nbsp;<code>nums1</code>&nbsp;和&nbsp;<code>nums2</code>&nbsp;。</p>

<p>请你返回 <code>nums1</code> 和 <code>nums2</code> 中两个长度相同的 <strong>非空</strong> 子序列的最大点积。</p>

<p>数组的非空子序列是通过删除原数组中某些元素（可能一个也不删除）后剩余数字组成的序列，但不能改变数字间相对顺序。比方说，<code>[2,3,5]</code>&nbsp;是&nbsp;<code>[1,2,3,4,5]</code>&nbsp;的一个子序列而&nbsp;<code>[1,5,3]</code>&nbsp;不是。</p>

<p>&nbsp;</p>

<p><strong>示例 1：</strong></p>

<pre>
<strong>输入：</strong>nums1 = [2,1,-2,5], nums2 = [3,0,-6]
<strong>输出：</strong>18
<strong>解释：</strong>从 nums1 中得到子序列 [2,-2] ，从 nums2 中得到子序列 [3,-6] 。
它们的点积为 (2*3 + (-2)*(-6)) = 18 。</pre>

<p><strong>示例 2：</strong></p>

<pre>
<strong>输入：</strong>nums1 = [3,-2], nums2 = [2,-6,7]
<strong>输出：</strong>21
<strong>解释：</strong>从 nums1 中得到子序列 [3] ，从 nums2 中得到子序列 [7] 。
它们的点积为 (3*7) = 21 。</pre>

<p><strong>示例 3：</strong></p>

<pre>
<strong>输入：</strong>nums1 = [-1,-1], nums2 = [1,1]
<strong>输出：</strong>-1
<strong>解释：</strong>从 nums1 中得到子序列 [-1] ，从 nums2 中得到子序列 [1] 。
它们的点积为 -1 。</pre>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li><code>1 &lt;= nums1.length, nums2.length &lt;= 500</code></li>
	<li><code>-1000 &lt;= nums1[i], nums2[i] &lt;= 1000</code></li>
</ul>

<p>&nbsp;</p>

<p><strong>点积：</strong></p>

<pre>
定义 <code><strong>a</strong>&nbsp;= [<em>a</em><sub>1</sub>,&nbsp;<em>a</em><sub>2</sub>,…,&nbsp;<em>a</em><sub><em>n</em></sub>]</code> 和<strong> <code>b</code></strong><code>&nbsp;= [<em>b</em><sub>1</sub>,&nbsp;<em>b</em><sub>2</sub>,…,&nbsp;<em>b</em><sub><em>n</em></sub>]</code> 的点积为：

<img alt="\mathbf{a}\cdot \mathbf{b} = \sum_{i=1}^n a_ib_i = a_1b_1 + a_2b_2 + \cdots + a_nb_n " class="tex" src="https://pic.leetcode.cn/1666164309-PBJMQp-image.png" />

这里的 <strong>Σ</strong> 指示总和符号。
</pre>


---

## 代码模板


### C++

```cpp
class Solution {
public:
    int maxDotProduct(vector<int>& nums1, vector<int>& nums2) {
        
    }
};
```

### Java

```java
class Solution {
    public int maxDotProduct(int[] nums1, int[] nums2) {
        
    }
}
```

### Python3

```python3
class Solution:
    def maxDotProduct(self, nums1: List[int], nums2: List[int]) -> int:
        
```

### JavaScript

```javascript
/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number}
 */
var maxDotProduct = function(nums1, nums2) {
    
};
```

### Go

```golang
func maxDotProduct(nums1 []int, nums2 []int) int {
    
}
```

---

## 提示

1. Use dynamic programming, define DP[i][j] as the maximum dot product of two subsequences starting in the position i of nums1 and position j of nums2.

---

## 示例测试用例

```
[2,1,-2,5]
[3,0,-6]
```

---

## AI 解答 (DeepSeek)

## 1️⃣ 题意理解

**题目重述**  
给定两个数组 `nums1` 和 `nums2`，需要从每个数组中分别选出一个**长度相同**的**非空子序列**，使得这两个子序列的**点积**（对应位置元素乘积之和）最大。

**关键约束**  
- 子序列：保持原数组顺序，但不要求连续  
- 两个子序列长度必须相同  
- 子序列必须非空  
- 数组长度 ≤ 500，元素值范围 [-1000, 1000]  
- 点积可能为负数（如示例3）

**输入输出格式**  
- 输入：两个整数数组 `nums1` 和 `nums2`  
- 输出：一个整数，表示最大点积

## 2️⃣ 解题思路

### 解法一：动态规划（二维DP）
**核心思想**  
类似最长公共子序列（LCS）的DP思路，但状态转移时计算点积。  
定义 `dp[i][j]` 表示 `nums1[0..i-1]` 和 `nums2[0..j-1]` 中选出的两个等长子序列的最大点积。  
状态转移考虑四种情况：
1. 选择 `nums1[i-1]` 和 `nums2[j-1]` 配对，点积为 `nums1[i-1]*nums2[j-1]`，可能加上之前的最大点积 `dp[i-1][j-1]`（若为正）
2. 不选 `nums1[i-1]`，即 `dp[i-1][j]`
3. 不选 `nums2[j-1]`，即 `dp[i][j-1]`
4. 只选当前两个数配对（相当于重新开始选子序列）

**注意事项**  
- 初始化时 `dp[0][j]` 和 `dp[i][0]` 设为负无穷（因为子序列必须非空）  
- 最终答案可能为负数（如示例3）  
- 需要处理负数的乘积可能使点积变大的情况

### 解法二：动态规划（空间优化）
**核心思想**  
由于 `dp[i][j]` 只依赖于 `dp[i-1][j-1]`、`dp[i-1][j]` 和 `dp[i][j-1]`，可以用滚动数组优化空间。

**注意事项**  
- 需要保存 `dp[i-1][j-1]` 的值，因为会被覆盖  
- 注意更新顺序

## 3️⃣ 代码实现

### 解法一：二维动态规划
```go
func maxDotProduct(nums1 []int, nums2 []int) int {
    m, n := len(nums1), len(nums2)
    // dp[i][j] 表示 nums1[0..i-1] 和 nums2[0..j-1] 的最大点积
    dp := make([][]int, m+1)
    for i := range dp {
        dp[i] = make([]int, n+1)
        for j := range dp[i] {
            dp[i][j] = -1 << 31 // 初始化为极小值
        }
    }
    
    for i := 1; i <= m; i++ {
        for j := 1; j <= n; j++ {
            // 情况1：只选当前两个数配对
            cur := nums1[i-1] * nums2[j-1]
            // 情况2：当前两个数配对，并加上之前的最大点积（如果为正）
            if dp[i-1][j-1] > 0 {
                cur += dp[i-1][j-1]
            }
            // 情况3：不选 nums1[i-1]
            // 情况4：不选 nums2[j-1]
            dp[i][j] = max(cur, max(dp[i-1][j], dp[i][j-1]))
        }
    }
    return dp[m][n]
}

func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}
```

### 解法二：空间优化动态规划
```go
func maxDotProduct(nums1 []int, nums2 []int) int {
    m, n := len(nums1), len(nums2)
    // 使用一维数组，dp[j] 表示当前行
    dp := make([]int, n+1)
    for j := range dp {
        dp[j] = -1 << 31
    }
    
    for i := 1; i <= m; i++ {
        // pre 保存 dp[i-1][j-1]
        pre := dp[0]
        dp[0] = -1 << 31 // 每行开始重置
        for j := 1; j <= n; j++ {
            tmp := dp[j] // 保存当前 dp[i-1][j] 给下一轮作为 pre
            cur := nums1[i-1] * nums2[j-1]
            if pre > 0 {
                cur += pre
            }
            dp[j] = max(cur, max(dp[j], dp[j-1]))
            pre = tmp // 更新 pre 为 dp[i-1][j]
        }
    }
    return dp[n]
}

func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}
```

## 4️⃣ 复杂度分析

### 解法一：二维DP
- ⏱️ **时间复杂度**：O(m×n)，其中 m 和 n 分别为两个数组的长度。需要填充 m×n 的DP表。
- 💾 **空间复杂度**：O(m×n)，用于存储DP表。

### 解法二：空间优化DP
- ⏱️ **时间复杂度**：O(m×n)，与解法一相同。
- 💾 **空间复杂度**：O(n)，只使用一维数组，n 为第二个数组的长度。

## 5️⃣ 解法对比

| 解法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 二维DP | 逻辑清晰，易于理解和调试 | 空间占用较大 | 数组长度较小（≤500）时完全可行 |
| 空间优化DP | 空间效率高，适合内存敏感场景 | 代码稍复杂，状态转移需要小心处理 | 数组长度较大或内存受限时 |

**推荐**  
本题数组长度最大为500，二维DP的空间复杂度 O(500×500) = O(250k) 在可接受范围内。推荐使用**解法一**，因为它逻辑更清晰，不易出错。在实际面试中，可以先实现解法一，再优化到解法二，展示优化能力。

## 6️⃣ 测试用例

### 用例1：常规用例
```
输入：nums1 = [2,1,-2,5], nums2 = [3,0,-6]
输出：18
解释：子序列 [2,-2] 和 [3,-6] 点积为 2*3 + (-2)*(-6) = 6 + 12 = 18
```

### 用例2：全负数情况
```
输入：nums1 = [-1,-1], nums2 = [1,1]
输出：-1
解释：只能选单个元素配对，最大点积为 (-1)*1 = -1
```

### 用例3：包含零和正负数
```
输入：nums1 = [3,-2], nums2 = [2,-6,7]
输出：21
解释：子序列 [3] 和 [7] 点积为 21
```

### 用例4：边界情况（长数组）
```
输入：nums1 = [1000]*500, nums2 = [1000]*500
输出：500000000
解释：每个数组选全部元素，点积 = 500 * (1000*1000) = 5e8
```

## 7️⃣ 相关题目
- **LeetCode 1143**：最长公共子序列（LCS）——类似DP结构
- **LeetCode 1035**：不相交的线——本质也是LCS
- **LeetCode 72**：编辑距离——二维DP状态转移
- **LeetCode 152**：乘积最大子数组——处理负数乘积的思路

---

*获取时间: 2026-03-18 10:33:05*
