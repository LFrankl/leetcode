# 263. 丑数

**难度**: Easy

**标签**: 数学

**链接**: https://leetcode.cn/problems/ugly-number/

---

## 题目描述

<p><strong>丑数 </strong>就是只包含质因数&nbsp;<code>2</code>、<code>3</code> 和 <code>5</code>&nbsp;的&nbsp;<em>正&nbsp;</em>整数。</p>

<p>给你一个整数 <code>n</code> ，请你判断 <code>n</code> 是否为 <strong>丑数</strong> 。如果是，返回 <code>true</code> ；否则，返回 <code>false</code> 。</p>

<p>&nbsp;</p>

<p><strong>示例 1：</strong></p>

<pre>
<strong>输入：</strong>n = 6
<strong>输出：</strong>true
<strong>解释：</strong>6 = 2 × 3</pre>

<p><strong>示例 2：</strong></p>

<pre>
<strong>输入：</strong>n = 1
<strong>输出：</strong>true
<strong>解释：</strong>1 没有质因数。</pre>

<p><strong>示例 3：</strong></p>

<pre>
<strong>输入：</strong>n = 14
<strong>输出：</strong>false
<strong>解释：</strong>14 不是丑数，因为它包含了另外一个质因数&nbsp;<code>7 </code>。
</pre>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li><code>-2<sup>31</sup> &lt;= n &lt;= 2<sup>31</sup> - 1</code></li>
</ul>


---

## 代码模板


### C++

```cpp
class Solution {
public:
    bool isUgly(int n) {
        
    }
};
```

### Java

```java
class Solution {
    public boolean isUgly(int n) {
        
    }
}
```

### Python3

```python3
class Solution:
    def isUgly(self, n: int) -> bool:
        
```

### JavaScript

```javascript
/**
 * @param {number} n
 * @return {boolean}
 */
var isUgly = function(n) {
    
};
```

### Go

```golang
func isUgly(n int) bool {
    
}
```

---

## 示例测试用例

```
6
```

---

## AI 解答 (DeepSeek)

### 解法一：不断除以 2、3、5

思路：如果 n 是丑数，那么不断除以 2、3、5 最终一定会得到 1。反之，如果除不尽（即 n 不再能被 2、3、5 整除）且 n ≠ 1，则说明 n 含有其他质因数，不是丑数。注意 n ≤ 0 直接返回 false。

```go
func isUgly(n int) bool {
    if n <= 0 {
        return false
    }
    for _, p := range []int{2, 3, 5} {
        for n%p == 0 {
            n /= p
        }
    }
    return n == 1
}
```

复杂度：时间 O(log n)，空间 O(1)

---

*获取时间: 2026-06-12 10:00:20*
