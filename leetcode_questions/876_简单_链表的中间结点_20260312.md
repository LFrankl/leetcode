# 876. 链表的中间结点

**难度**: Easy

**标签**: 链表, 双指针

**链接**: https://leetcode.cn/problems/middle-of-the-linked-list/

---

## 题目描述

<p>给你单链表的头结点 <code>head</code> ，请你找出并返回链表的中间结点。</p>

<p>如果有两个中间结点，则返回第二个中间结点。</p>

<p>&nbsp;</p>

<p><strong class="example">示例 1：</strong></p>
<img alt="" src="https://assets.leetcode.com/uploads/2021/07/23/lc-midlist1.jpg" style="width: 544px; height: 65px;" />
<pre>
<strong>输入：</strong>head = [1,2,3,4,5]
<strong>输出：</strong>[3,4,5]
<strong>解释：</strong>链表只有一个中间结点，值为 3 。
</pre>

<p><strong class="example">示例 2：</strong></p>
<img alt="" src="https://assets.leetcode.com/uploads/2021/07/23/lc-midlist2.jpg" style="width: 664px; height: 65px;" />
<pre>
<strong>输入：</strong>head = [1,2,3,4,5,6]
<strong>输出：</strong>[4,5,6]
<strong>解释：</strong>该链表有两个中间结点，值分别为 3 和 4 ，返回第二个结点。
</pre>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li>链表的结点数范围是 <code>[1, 100]</code></li>
	<li><code>1 &lt;= Node.val &lt;= 100</code></li>
</ul>


---

## 代码模板


### C++

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* middleNode(ListNode* head) {
        
    }
};
```

### Java

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode middleNode(ListNode head) {
        
    }
}
```

### Python3

```python3
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:
        
```

### JavaScript

```javascript
/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {ListNode}
 */
var middleNode = function(head) {
    
};
```

### Go

```golang
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func middleNode(head *ListNode) *ListNode {
    
}
```

---

## 示例测试用例

```
[1,2,3,4,5]
```

---

## AI 解答 (DeepSeek)

# 链表的中间结点 - 解题分析

## 1️⃣ 题意理解

**核心要求**：
给定一个单链表的头节点 `head`，需要找到并返回链表的中间节点。如果链表有偶数个节点（即存在两个中间节点），则返回**第二个**中间节点。

**关键约束**：
- 链表节点数范围：`[1, 100]`
- 节点值范围：`[1, 100]`
- 链表非空（至少有一个节点）

**边界情况**：
1. 链表只有一个节点：直接返回该节点
2. 链表有两个节点：返回第二个节点
3. 链表有偶数个节点：返回第二个中间节点
4. 链表有奇数个节点：返回唯一的中间节点

**输入输出格式**：
- 输入：单链表的头节点 `head`
- 输出：中间节点（从该节点开始的后半部分链表）

## 2️⃣ 解题思路

### 解法一：两次遍历法
**核心思想**：先遍历一次链表获取长度，再遍历到中间位置
- 第一次遍历：计算链表长度 `n`
- 第二次遍历：移动到第 `n/2` 个节点（从0开始计数）

### 解法二：快慢指针法（推荐）
**核心思想**：使用两个指针，快指针每次走两步，慢指针每次走一步
- 当快指针到达链表末尾时，慢指针正好在中间位置
- 对于偶数个节点的情况，快指针会停在最后一个节点，慢指针停在第二个中间节点

### 解法三：数组存储法
**核心思想**：将链表节点存储到数组中，直接通过索引访问中间节点
- 遍历链表，将每个节点存入数组
- 通过数组长度计算中间索引：`len(arr)/2`

## 3️⃣ 代码实现

### 解法一：两次遍历法
```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func middleNode(head *ListNode) *ListNode {
    // 第一次遍历：计算链表长度
    length := 0
    current := head
    for current != nil {
        length++
        current = current.Next
    }
    
    // 第二次遍历：移动到中间位置
    middleIndex := length / 2
    current = head
    for i := 0; i < middleIndex; i++ {
        current = current.Next
    }
    
    return current
}
```

### 解法二：快慢指针法（推荐）
```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func middleNode(head *ListNode) *ListNode {
    // 初始化快慢指针
    slow, fast := head, head
    
    // 快指针每次走两步，慢指针每次走一步
    // 当快指针到达末尾时，慢指针就在中间
    for fast != nil && fast.Next != nil {
        slow = slow.Next      // 慢指针走一步
        fast = fast.Next.Next // 快指针走两步
    }
    
    return slow
}
```

### 解法三：数组存储法
```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func middleNode(head *ListNode) *ListNode {
    // 将链表节点存储到数组中
    nodes := make([]*ListNode, 0)
    current := head
    
    for current != nil {
        nodes = append(nodes, current)
        current = current.Next
    }
    
    // 直接通过索引访问中间节点
    return nodes[len(nodes)/2]
}
```

## 4️⃣ 复杂度分析

### 解法一：两次遍历法
- ⏱️ **时间复杂度**：O(n)
  - 第一次遍历：O(n) 计算长度
  - 第二次遍历：O(n/2) ≈ O(n) 移动到中间
  - 总时间复杂度：O(n) + O(n) = O(n)
- 💾 **空间复杂度**：O(1)
  - 只使用了常数级别的额外空间（几个指针变量）

### 解法二：快慢指针法
- ⏱️ **时间复杂度**：O(n)
  - 只需要遍历一次链表
  - 快指针走到末尾时，慢指针正好在中间
- 💾 **空间复杂度**：O(1)
  - 只使用了两个指针变量，常数空间

### 解法三：数组存储法
- ⏱️ **时间复杂度**：O(n)
  - 遍历一次链表存储节点
  - 索引访问是O(1)
- 💾 **空间复杂度**：O(n)
  - 需要额外的数组存储所有节点
  - 空间复杂度与链表长度成正比

## 5️⃣ 解法对比

| 解法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 两次遍历法 | 思路简单直观，容易实现 | 需要遍历两次链表，效率较低 | 教学示例，小规模数据 |
| 快慢指针法 | **只需遍历一次**，效率高，空间最优 | 指针移动逻辑需要理解 | **推荐使用**，各种规模数据 |
| 数组存储法 | 实现简单，直接索引访问 | 需要额外O(n)空间 | 需要多次访问中间节点的情况 |

**推荐解法**：快慢指针法
- 时间复杂度最优（只需一次遍历）
- 空间复杂度最优（O(1)）
- 代码简洁优雅
- 是链表中间节点问题的标准解法

## 6️⃣ 测试用例

### 测试用例1：奇数长度链表
```
输入：1 → 2 → 3 → 4 → 5
输出：3 → 4 → 5
解释：链表有5个节点，中间节点是第3个节点（值为3）
```

### 测试用例2：偶数长度链表
```
输入：1 → 2 → 3 → 4 → 5 → 6
输出：4 → 5 → 6
解释：链表有6个节点，有两个中间节点（3和4），返回第二个中间节点（值为4）
```

### 测试用例3：最小边界情况
```
输入：1
输出：1
解释：链表只有一个节点，该节点就是中间节点
```

### 测试用例4：两个节点的情况
```
输入：1 → 2
输出：2
解释：链表有两个节点，返回第二个节点
```

## 7️⃣ 相关题目

1. **876. 链表的中间结点**（本题）
2. **234. 回文链表** - 需要找到中间节点，然后反转后半部分进行比较
3. **141. 环形链表** - 同样使用快慢指针法判断链表是否有环
4. **142. 环形链表 II** - 快慢指针法的进阶应用，需要找到环的入口
5. **143. 重排链表** - 需要找到中间节点，然后合并两个链表

这些题目都涉及链表的遍历和指针操作，特别是快慢指针技巧在链表问题中非常常用。

---

*获取时间: 2026-03-12 10:31:06*
