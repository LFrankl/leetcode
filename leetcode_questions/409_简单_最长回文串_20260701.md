# 409. 最长回文串

**难度**: Easy

**标签**: 贪心, 哈希表, 字符串

**链接**: https://leetcode.cn/problems/longest-palindrome/

---

## 题目描述

<p>给定一个包含大写字母和小写字母的字符串<meta charset="UTF-8" />&nbsp;<code>s</code>&nbsp;，返回&nbsp;<em>通过这些字母构造成的 <strong>最长的 <span data-keyword="palindrome-string">回文串</span></strong></em>&nbsp;的长度。</p>

<p>在构造过程中，请注意 <strong>区分大小写</strong> 。比如&nbsp;<code>"Aa"</code>&nbsp;不能当做一个回文字符串。</p>

<p>&nbsp;</p>

<p><strong class="example">示例 1: </strong></p>

<pre>
<strong>输入:</strong>s = "abccccdd"
<strong>输出:</strong>7
<strong>解释:</strong>
我们可以构造的最长的回文串是"dccaccd", 它的长度是 7。
</pre>

<p><strong class="example">示例 2:</strong></p>

<pre>
<strong>输入:</strong>s = "a"
<strong>输出:</strong>1
<strong>解释：</strong>可以构造的最长回文串是"a"，它的长度是 1。
</pre>

<p>&nbsp;</p>

<p><strong>提示:</strong></p>

<ul>
	<li><code>1 &lt;= s.length &lt;= 2000</code></li>
	<li><code>s</code>&nbsp;只由小写 <strong>和/或</strong> 大写英文字母组成</li>
</ul>


---

## 代码模板


### C++

```cpp
class Solution {
public:
    int longestPalindrome(string s) {
        
    }
};
```

### Java

```java
class Solution {
    public int longestPalindrome(String s) {
        
    }
}
```

### Python3

```python3
class Solution:
    def longestPalindrome(self, s: str) -> int:
        
```

### JavaScript

```javascript
/**
 * @param {string} s
 * @return {number}
 */
var longestPalindrome = function(s) {
    
};
```

### Go

```golang
func longestPalindrome(s string) int {
    
}
```

---

## 示例测试用例

```
"abccccdd"
```

---

## AI 解答 (DeepSeek)

### 解法一：贪心 + 字符计数

思路：回文串左右对称，中间可放一个单独字符。统计每个字符出现次数，偶数次全部可用，奇数次取最大偶数部分（`count - 1`），最后若存在奇数次字符则中心再加 1。

```go
func longestPalindrome(s string) int {
    count := make(map[rune]int)
    for _, ch := range s {
        count[ch]++
    }
    
    length := 0
    hasOdd := false
    for _, freq := range count {
        length += freq / 2 * 2  // 取偶数部分
        if freq % 2 == 1 {
            hasOdd = true
        }
    }
    if hasOdd {
        length += 1  // 中间放一个奇数字符
    }
    return length
}
```

复杂度：时间 O(n)，空间 O(k)（k 为字符种类数，最多52）

---

### 解法二：贪心 + 数组计数（最优）

思路：与解法一相同逻辑，但用固定大小数组替代哈希表，利用 ASCII 值直接映射索引，减少哈希开销。

```go
func longestPalindrome(s string) int {
    freq := make([]int, 128)
    for _, ch := range s {
        freq[ch]++
    }
    
    length := 0
    for _, f := range freq {
        length += f / 2 * 2
        if f % 2 == 1 && length % 2 == 0 {
            length += 1
        }
    }
    return length
}
```

复杂度：时间 O(n)，空间 O(1)（固定128大小数组）

> **核心要点**：贪心策略——偶数全用，奇数留一个作中心。解法二用 `length % 2 == 0` 判断是否已添加中心字符，更简洁且只需一次遍历 freq 数组。

---

*获取时间: 2026-07-01 10:00:18*
