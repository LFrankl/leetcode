# 3099. 哈沙德数

**难度**: Easy

**标签**: 数学

**链接**: https://leetcode.cn/problems/harshad-number/

---

## 题目描述

<p>如果一个整数能够被其各个数位上的数字之和整除，则称之为<strong> 哈沙德数</strong>（Harshad number）。给你一个整数 <code>x</code> 。如果 <code>x</code> 是 <strong>哈沙德数 </strong>，则返回<em> </em><code>x</code> 各个数位上的数字之和，否则，返回<em> </em><code>-1</code> 。</p>

<p>&nbsp;</p>

<p><strong class="example">示例 1：</strong></p>

<div class="example-block">
<p><strong>输入：</strong> <span class="example-io">x = 18</span></p>

<p><strong>输出：</strong> <span class="example-io">9</span></p>

<p><strong>解释：</strong></p>

<p><code>x</code> 各个数位上的数字之和为 <code>9</code> 。<code>18</code> 能被 <code>9</code> 整除。因此 <code>18</code> 是哈沙德数，答案是 <code>9</code> 。</p>
</div>

<p><strong class="example">示例 2：</strong></p>

<div class="example-block">
<p><strong>输入：</strong> <span class="example-io">x = 23</span></p>

<p><strong>输出：</strong> <span class="example-io">-1</span></p>

<p><strong>解释：</strong></p>

<p><code>x</code> 各个数位上的数字之和为 <code>5</code> 。<code>23</code> 不能被 <code>5</code> 整除。因此 <code>23</code> 不是哈沙德数，答案是 <code>-1</code> 。</p>
</div>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li><code>1 &lt;= x &lt;= 100</code></li>
</ul>


---

## 代码模板


### C++

```cpp
class Solution {
public:
    int sumOfTheDigitsOfHarshadNumber(int x) {
        
    }
};
```

### Java

```java
class Solution {
    public int sumOfTheDigitsOfHarshadNumber(int x) {
        
    }
}
```

### Python3

```python3
class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        
```

### JavaScript

```javascript
/**
 * @param {number} x
 * @return {number}
 */
var sumOfTheDigitsOfHarshadNumber = function(x) {
    
};
```

### Go

```golang
func sumOfTheDigitsOfHarshadNumber(x int) int {
    
}
```

---

## 提示

1. Use a while loop and divide <code>x</code> by <code>10</code> to find the sum of the digits of <code>x</code>.

---

## 示例测试用例

```
18
```

---

## AI 解答 (DeepSeek)

## 1️⃣ 题意理解

**题目重述**：  
给定一个整数 `x`，判断它是否为哈沙德数（即能否被其各位数字之和整除）。如果是，返回各位数字之和；否则返回 `-1`。

**关键约束**：
- `1 <= x <= 100`，数据范围很小，可以直接暴力计算。
- 注意 `x` 是正整数，无需处理负数或零。
- 整除条件：`x % sumDigits == 0`。

**输入输出**：
- 输入：整数 `x`
- 输出：整数（数字之和 或 `-1`）

---

## 2️⃣ 解题思路

### 解法一：直接模拟
1. 计算 `x` 的各位数字之和。
2. 判断 `x` 是否能被该和整除。
3. 根据结果返回相应值。

### 解法二：数学优化（针对本题范围不必要，但可扩展）
- 对于更大范围的 `x`，计算数字之和时可以用循环取模，避免转字符串开销。
- 本题范围很小，直接模拟即可。

---

## 3️⃣ 代码实现

### 解法一：直接模拟
```go
func sumOfTheDigitsOfHarshadNumber(x int) int {
    // 计算各位数字之和
    sum := 0
    temp := x
    for temp > 0 {
        sum += temp % 10
        temp /= 10
    }
    
    // 判断是否为哈沙德数
    if x % sum == 0 {
        return sum
    }
    return -1
}
```

### 解法二：使用字符串转换（仅作对比，不推荐）
```go
func sumOfTheDigitsOfHarshadNumber2(x int) int {
    // 将数字转为字符串，遍历每个字符计算和
    s := strconv.Itoa(x)
    sum := 0
    for _, ch := range s {
        sum += int(ch - '0')
    }
    
    if x % sum == 0 {
        return sum
    }
    return -1
}
```

---

## 4️⃣ 复杂度分析

### 解法一：直接模拟
- ⏱️ **时间复杂度**：O(log₁₀ x)  
  因为循环次数等于 `x` 的十进制位数，最多 3 次（x ≤ 100）。
- 💾 **空间复杂度**：O(1)  
  只用了常数个变量。

### 解法二：字符串转换
- ⏱️ **时间复杂度**：O(log₁₀ x)  
  遍历字符串长度，同样最多 3 次。
- 💾 **空间复杂度**：O(log₁₀ x)  
  需要额外存储字符串，但本题范围很小，可忽略。

---

## 5️⃣ 解法对比

| 解法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 直接模拟 | 效率高，空间省，逻辑清晰 | 无 | 通用，尤其适合数值计算 |
| 字符串转换 | 代码直观，易于理解 | 额外空间开销，效率略低 | 教学或快速原型 |

**推荐解法一**：  
- 本题范围小，两种方法均可，但直接模拟更符合算法竞赛习惯。
- 字符串转换涉及类型转换和内存分配，在性能敏感场景不推荐。

---

## 6️⃣ 测试用例

1. **常规用例**  
   - 输入：`x = 18` → 输出：`9`  
     解释：1+8=9，18%9=0，是哈沙德数。
   - 输入：`x = 23` → 输出：`-1`  
     解释：2+3=5，23%5≠0，不是哈沙德数。

2. **边界用例**  
   - 输入：`x = 1` → 输出：`1`  
     解释：1%1=0，是哈沙德数。
   - 输入：`x = 100` → 输出：`-1`  
     解释：1+0+0=1，100%1=0，是哈沙德数，返回 1（注意：这里返回 1，不是 -1）。

3. **特殊用例**  
   - 输入：`x = 10` → 输出：`-1`  
     解释：1+0=1，10%1=0，是哈沙德数，返回 1（注意：这里返回 1，不是 -1）。

---

## 7️⃣ 相关题目

- **LeetCode 258. 各位相加**（Add Digits）  
  计算数字的各位之和，直到结果为一位数。
- **LeetCode 202. 快乐数**（Happy Number）  
  涉及数字各位平方和的循环判断。
- **LeetCode 507. 完美数**（Perfect Number）  
  判断一个数是否等于其真因子之和。

---

*获取时间: 2026-03-03 19:15:07*
