# 2774. 数组的上界

**难度**: Easy

**标签**: 

**链接**: https://leetcode.cn/problems/array-upper-bound/

---

## 题目描述

None

---

## 代码模板


---

## 提示

1. Inside the Array.prototype.upperBound function you have access to the "this" keyword. You can access array elements, values, and methods. For example "this[0]", "this[1]", "this.length", "this.map()", etc.
2. The most efficient way to solve this problem is with binary search.
3. Choose the middle element and check if it's less than or equal to the goal value. If so, you can rule out the left side of the array.

---

## 示例测试用例

```
[3,4,5]
5
```

---

## AI 解答 (DeepSeek)

## 1️⃣ 题意理解

**题目重述**：
给定一个已按升序排列的整数数组 `nums` 和一个目标值 `target`，要求找到 `target` 在数组中的**上界**。上界定义为第一个大于 `target` 的元素索引。如果所有元素都不大于 `target`，则返回数组长度。

**关键约束**：
- 数组已按升序排列
- 数组可能为空
- 元素可能重复
- 需要找到第一个**大于** `target` 的位置，而不是等于

**边界情况**：
- 空数组：返回 0
- 所有元素 ≤ target：返回数组长度
- 所有元素 > target：返回 0
- 有多个等于 target 的元素：返回第一个大于 target 的位置

**输入输出**：
- 输入：`nums []int`, `target int`
- 输出：`int` 类型，表示上界索引

## 2️⃣ 解题思路

### 解法一：线性扫描
**核心思想**：从左到右遍历数组，找到第一个大于 `target` 的元素。

**步骤**：
1. 遍历数组中的每个元素
2. 如果当前元素 > target，返回当前索引
3. 如果遍历结束都没有找到，返回数组长度

**注意事项**：
- 需要处理空数组情况
- 注意边界条件

### 解法二：二分查找
**核心思想**：利用数组有序的特性，使用二分查找加速搜索。

**步骤**：
1. 初始化左右指针 `left = 0`, `right = len(nums)`
2. 当 `left < right` 时循环：
   - 计算中点 `mid = left + (right - left)/2`
   - 如果 `nums[mid] > target`，说明上界在左半部分，`right = mid`
   - 否则，说明上界在右半部分，`left = mid + 1`
3. 返回 `left` 即为上界位置

**注意事项**：
- 循环条件是 `left < right` 而不是 `left ≤ right`
- 中点计算防止溢出
- 最终返回的是 `left` 而不是 `mid`

## 3️⃣ 代码实现

### 解法一：线性扫描
```go
func upperBound(nums []int, target int) int {
    // 遍历数组
    for i, num := range nums {
        // 找到第一个大于 target 的元素
        if num > target {
            return i
        }
    }
    // 所有元素都不大于 target
    return len(nums)
}
```

### 解法二：二分查找
```go
func upperBound(nums []int, target int) int {
    left, right := 0, len(nums)
    
    // 二分查找
    for left < right {
        mid := left + (right-left)/2  // 防止溢出
        
        if nums[mid] > target {
            // 中点大于 target，上界在左半部分（包含中点）
            right = mid
        } else {
            // 中点小于等于 target，上界在右半部分
            left = mid + 1
        }
    }
    
    // left 即为第一个大于 target 的位置
    return left
}
```

## 4️⃣ 复杂度分析

### 解法一：线性扫描
- ⏱️ **时间复杂度**：O(n)
  - 最坏情况下需要遍历整个数组
  - n 为数组长度
- 💾 **空间复杂度**：O(1)
  - 只使用了常数级别的额外空间

### 解法二：二分查找
- ⏱️ **时间复杂度**：O(log n)
  - 每次将搜索范围减半
  - 最多需要 log₂n 次比较
- 💾 **空间复杂度**：O(1)
  - 只使用了几个指针变量

## 5️⃣ 解法对比

| 特性 | 线性扫描 | 二分查找 |
|------|----------|----------|
| 时间复杂度 | O(n) | O(log n) |
| 空间复杂度 | O(1) | O(1) |
| 代码复杂度 | 简单 | 中等 |
| 适用场景 | 小规模数据 | 大规模数据 |
| 稳定性 | 稳定 | 稳定 |

**推荐解法**：二分查找
- 数组已排序，二分查找是最优选择
- 时间复杂度从 O(n) 降到 O(log n)
- 对于大数据量优势明显

## 6️⃣ 测试用例

### 常规用例
```go
// 用例1：正常情况
nums := []int{1, 2, 4, 4, 5, 6}
target := 4
// 输出：4 → 第一个大于4的元素5在索引4

// 用例2：target不存在于数组中
nums := []int{1, 3, 5, 7}
target := 4
// 输出：2 → 第一个大于4的元素5在索引2
```

### 边界用例
```go
// 用例3：空数组
nums := []int{}
target := 5
// 输出：0

// 用例4：所有元素都大于target
nums := []int{5, 6, 7, 8}
target := 2
// 输出：0 → 第一个元素就大于target

// 用例5：所有元素都不大于target
nums := []int{1, 2, 3, 4}
target := 5
// 输出：4 → 数组长度

// 用例6：有重复元素
nums := []int{1, 2, 2, 2, 3, 4}
target := 2
// 输出：4 → 第一个大于2的元素3在索引4
```

## 7️⃣ 相关题目

1. **LeetCode 35. 搜索插入位置** - 类似但找的是插入位置（≥ target）
2. **LeetCode 34. 在排序数组中查找元素的第一个和最后一个位置** - 找上下界
3. **LeetCode 704. 二分查找** - 基础的二分查找
4. **LeetCode 278. 第一个错误的版本** - 类似的二分查找变体

---

*获取时间: 2026-03-25 10:31:08*
