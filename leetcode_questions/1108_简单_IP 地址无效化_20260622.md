# 1108. IP 地址无效化

**难度**: Easy

**标签**: 字符串

**链接**: https://leetcode.cn/problems/defanging-an-ip-address/

---

## 题目描述

<p>给你一个有效的 <a href="https://baike.baidu.com/item/IPv4" target="_blank">IPv4</a> 地址&nbsp;<code>address</code>，返回这个 IP 地址的无效化版本。</p>

<p>所谓无效化&nbsp;IP 地址，其实就是用&nbsp;<code>&quot;[.]&quot;</code>&nbsp;代替了每个 <code>&quot;.&quot;</code>。</p>

<p>&nbsp;</p>

<p><strong>示例 1：</strong></p>

<pre><strong>输入：</strong>address = &quot;1.1.1.1&quot;
<strong>输出：</strong>&quot;1[.]1[.]1[.]1&quot;
</pre>

<p><strong>示例 2：</strong></p>

<pre><strong>输入：</strong>address = &quot;255.100.50.0&quot;
<strong>输出：</strong>&quot;255[.]100[.]50[.]0&quot;
</pre>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li>给出的&nbsp;<code>address</code>&nbsp;是一个有效的 IPv4 地址</li>
</ul>


---

## 代码模板


### C++

```cpp
class Solution {
public:
    string defangIPaddr(string address) {
        
    }
};
```

### Java

```java
class Solution {
    public String defangIPaddr(String address) {
        
    }
}
```

### Python3

```python3
class Solution:
    def defangIPaddr(self, address: str) -> str:
        
```

### JavaScript

```javascript
/**
 * @param {string} address
 * @return {string}
 */
var defangIPaddr = function(address) {
    
};
```

### Go

```golang
func defangIPaddr(address string) string {
    
}
```

---

## 示例测试用例

```
"1.1.1.1"
```

---

## AI 解答 (DeepSeek)

### 解法一：字符串替换

思路：直接将每个 "." 替换为 "[.]"。Go 标准库的 `strings.Replace` 可以一步完成。

```go
func defangIPaddr(address string) string {
    return strings.Replace(address, ".", "[.]", -1)
}
```

复杂度：时间 O(n)，空间 O(n)

---

### 解法二：手动遍历构建

思路：遍历字符串，遇到 "." 时追加 "[.]"，否则追加原字符。避免依赖库函数，本质相同。

```go
func defangIPaddr(address string) string {
    var res []byte
    for _, ch := range address {
        if ch == '.' {
            res = append(res, '[', '.', ']')
        } else {
            res = append(res, byte(ch))
        }
    }
    return string(res)
}
```

复杂度：时间 O(n)，空间 O(n)

> 两种解法效率一致，解法一更简洁，解法二更直观。

---

*获取时间: 2026-06-22 10:00:12*
